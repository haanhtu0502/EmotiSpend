import 'package:flutter/material.dart';

const ColorScheme lightColorScheme = ColorScheme.light(
  brightness: Brightness.light,
  primary: Color(0xFFFF5FA2), // Hồng chủ đạo
  onPrimary: Colors.white,
  secondary: Color(0xFF4FC3F7), // Xanh dương pastel
  onSecondary: Colors.white,
  tertiary: Color(0xFF4ADE80), // Xanh lá
  error: Color(0xFFFF7C6E), // Cam/đỏ nhạt
  onError: Colors.white,
  background: Colors.white,
  onBackground: Color(0xFF6B7280), // Xám chữ phụ
  surface: Colors.white,
  onSurface: Color(0xFF111827), // Xám đậm cho text chính
);

const jarColors = [
  Color(0xFF22C55E), // Xanh lá
  Color(0xFFFACC15), // Vàng
  Color(0xFF3B82F6), // Xanh dương
  Color(0xFFA855F7), // Tím
  Color(0xFFEC4899), // Hồng
  Color(0xFFF43F5E), // Đỏ
];
